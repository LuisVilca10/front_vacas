import 'package:flutter/material.dart';

class Usuariio extends StatelessWidget {
  const Usuariio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}